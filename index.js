import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // Update the import statement to point to the correct location of the App component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
